name: Docker Image CI

on:
  push:
    branches: [ feat/docker ]
  pull_request:
    branches: [ feat/docker ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        ref: feat/docker
    - name: Build the Docker image 
      run: |
        docker build . --file dockerfile --tag filvenus/venus-market:latest
        # docker tag filvenus/venus-market:latest filvenus/venus-market:${{steps.vars.outputs.tag}}
    - name: Login to registry
      run: docker login --username=filvenus --password ${{ secrets.DOCKER_PASSWORD }} 
    - name: Push Image
      run: |
        # docker push filvenus/venus-market:${{steps.vars.outputs.tag}}
        docker push filvenus/venus-market:latest